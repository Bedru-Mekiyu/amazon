
{
  "name": "amazon-clone",
  "private": true,
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "build:prod": "npm install --workspaces && cd frontend && npm run build && cd ../backend && npm install --production",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}