services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    # For MySQL/PostgreSQL, add DB config and a separate db service:
    # depends_on:
    #   - db
    # environment:
    #   - DB_TYPE=postgres
    #   - RDS_HOSTNAME=db
    #   - RDS_PORT=5432
    #   - RDS_DB_NAME=amazon
    #   - RDS_USERNAME=amazon
    #   - RDS_PASSWORD=amazon
    #
    # db:
    #   image: postgres:16-alpine
    #   environment:
    #     POSTGRES_DB: amazon
    #     POSTGRES_USER: amazon
    #     POSTGRES_PASSWORD: amazon
    #   ports:
    #     - "5432:5432"
    #   volumes:
    #     - pgdata:/var/lib/postgresql/data
    #
    # volumes:
    #   pgdata:
