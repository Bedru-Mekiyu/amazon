{
  "name": "amazon-clone",
  "private": true,
  "scripts": {
    "build:prod": "cd frontend && npm install && npm run build && cd ../backend && npm install --production",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}