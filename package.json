{
  "name": "amazon-clone",
  "private": true,
  "scripts": {
    "build:prod": "cd frontend && npm ci && npm run build && cd ../backend && npm ci --production",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}