{
  "name": "amazon-clone",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build:prod": "npm ci --workspaces && npm run build --workspace=frontend && npm ci --workspace=backend --production",
    "start": "node backend/server.js"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=18"
  }
}